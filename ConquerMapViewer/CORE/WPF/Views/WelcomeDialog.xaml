<Window x:Class="ConquerMapViewer.WPF.Views.WelcomeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome to Conquer Map Viewer" 
        Height="350" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Welcome to Conquer Map Viewer!" 
                   FontSize="20" 
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" 
                   TextWrapping="Wrap"
                   Margin="0,0,0,20">
            <Run Text="To get started, please select your Conquer Online installation directory."/>
            <LineBreak/>
            <Run Text="This directory should contain the game files including maps, textures, and data files."/>
        </TextBlock>

        <!-- Directory Selection -->
        <TextBlock Grid.Row="2" 
                   Text="Conquer Online Directory:" 
                   FontWeight="Bold"
                   Margin="0,0,0,5"/>

        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="DirectoryPathTextBox" 
                     Grid.Column="0"
                     VerticalContentAlignment="Center"
                     Padding="5"
                     TextChanged="DirectoryPathTextBox_TextChanged"/>

            <Button x:Name="BrowseButton"
                    Grid.Column="1"
                    Content="Browse..."
                    Width="80"
                    Margin="5,0,0,0"
                    Click="BrowseButton_Click"/>
        </Grid>

        <!-- Auto-detect Button -->
        <Button Grid.Row="4"
                Content="🔍 Try Common Locations"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Padding="10,5"
                Click="TryCommonLocations_Click"
                ToolTip="Automatically search for Conquer Online in common installation locations"/>

        <!-- Status -->
        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="5"
                   Text="Please select a directory"
                   Foreground="Gray"
                   Margin="0,10,0,10"
                   FontStyle="Italic"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="OkButton"
                    Content="OK" 
                    Width="80" 
                    Margin="0,0,10,0"
                    IsEnabled="False"
                    IsDefault="True"
                    Click="OkButton_Click"/>
            <Button x:Name="CancelButton"
                    Content="Cancel" 
                    Width="80"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
